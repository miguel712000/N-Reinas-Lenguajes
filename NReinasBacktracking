#lang racket

;(define listaN
;  (lambda (n lista)
;    (cond ((zero? n) lista)
;          (else (listaN (- n 1) (append '(-1) lista))))))

(define reinas
  (lambda(n)
    (reinasAux '() n)))

;(define reinasAux
;  (lambda ()
    